<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="android:textColorPrimaryInverse">@color/colorBlueText</item>
        <item name="android:colorBackground">@color/colorWhiteBackground</item>
        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="android:textColorSecondaryInverse">@color/colorAccent</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="android:textColorPrimaryInverse">@color/colorBlueText</item>
        <item name="android:colorBackground">@color/colorWhiteBackground</item>
        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="android:textColorSecondaryInverse">@color/colorAccent</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorPrimary">@color/colorWhiteBackground</item>
        <item name="android:colorPrimaryDark">@color/colorWhiteBackground</item>

    </style>


    <!-- Кастомная AppTheme.Launcher. Устанавливает в качестве фона вашу drawable -->
    <style name="AppTheme.Launcher" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <!-- для Android 5+ вы можете поменять цвет colorPrimaryDark, чтобы соответствовать цвету windowBackground -->
        <!-- <item name="colorPrimaryDark">@android:color/white</item> -->
    </style>
</resources>
